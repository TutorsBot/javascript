sampArr = [22, 55, 77];

// sample = [
//     {name : "Fazlur", course: "Full Stack"},
//     {name : "Anish", course : "Full Stack"}
// ]

// Adding value to the array
// sampArr.push(99);
// console.log(sampArr);

// Removing last from the array
// sampArr.pop();
// console.log(sampArr);

// Remove item from the beginning
// sampArr.shift();
// console.log(sampArr);

// Add an item to the beginning
// sampArr.unshift(11);
// console.log(sampArr);

// delete keyword for the array 
// delete sampArr[0];
// console.log(sampArr);

// Splice remove item from the starting position and no of item need to be deleted
// sampArr.splice(1, 2);
// console.log(sampArr);

// Splice Method
// sampArr.splice(0, 2, "sample", "text", "here");
// console.log(sampArr);

// slice method
// sampArr.slice(1, 2);
// console.log(sampArr);

// Concat Array Method
// sampArr.concat([34, 55, 66]);
// console.log(sampArr);
// console.log(sampArr.concat([34, 55, 66]));
// sampArr = sampArr.concat([34, 55, 66]);
// console.log(sampArr);


// sampArr.forEach((a, i, arr)=>{
//     console.log(a, i, arr);
// });